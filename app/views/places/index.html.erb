<% @places.each do |place| %>
  <div class="nice-box col-xs-10 col-xs-offset-1">
    <h1 class=stylish-link><%= link_to place.name, place_path(place) %></h1><br/>
    <p><%= place.description %></p><br/>
    <p><%= place.address %></p>

  </div>
<% end %>
<%= will_paginate @posts %>
